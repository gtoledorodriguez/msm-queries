<h1>List of directors</h1>

<table border="1">
  <tr>
    <th>ID</th>
    <th>Image</th>
    <th>Name</th>
    <th>Dob</th>
    <th></th>
  </tr>
  
  <% @list_of_directors = Director.all %>
  <% @list_of_directors.order({:dob => :desc}).each do |a_director| %>
  <tr>
    <td><%= a_director.id %></td>
    <td>
      <img src="<%= a_director.image %>" alt="<%= a_director.name %>" width="100">
    </td>
    <td><%= a_director.name %></td>
    <td><%= a_director.dob %></td>
    <td>
      <a href="/directors/<%= a_director.id %>">Show details</a>
    </td>
  </tr>
  <% end %>
</table>
